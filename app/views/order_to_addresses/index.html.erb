<h1>Listing order_to_addresses</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_to_addresses.each do |order_to_address| %>
      <tr>
        <td><%= link_to 'Show', order_to_address %></td>
        <td><%= link_to 'Edit', edit_order_to_address_path(order_to_address) %></td>
        <td><%= link_to 'Destroy', order_to_address, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order to address', new_order_to_address_path %>
