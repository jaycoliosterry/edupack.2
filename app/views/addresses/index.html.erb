<h1>Listing addresses</h1>

<table>
  <thead>
    <tr>
      <th>Company name</th>
      <th>Contact name</th>
      <th>Job title</th>
      <th>Department</th>
      <th>Telephone</th>
      <th>Fax</th>
      <th>Email</th>
      <th>Street1</th>
      <th>Street2</th>
      <th>City</th>
      <th>State</th>
      <th>Country</th>
      <th>Zip</th>
      <th>Address group</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @addresses.each do |address| %>
      <tr>
        <td><%= address.company_name %></td>
        <td><%= address.contact_name %></td>
        <td><%= address.job_title %></td>
        <td><%= address.department %></td>
        <td><%= address.telephone %></td>
        <td><%= address.fax %></td>
        <td><%= address.email %></td>
        <td><%= address.street1 %></td>
        <td><%= address.street2 %></td>
        <td><%= address.city %></td>
        <td><%= address.state %></td>
        <td><%= address.country %></td>
        <td><%= address.zip %></td>
        <td><%= address.address_group %></td>
        <td><%= link_to 'Show', address %></td>
        <td><%= link_to 'Edit', edit_address_path(address) %></td>
        <td><%= link_to 'Destroy', address, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Address', new_address_path %>
